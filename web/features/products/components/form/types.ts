export interface FormState {
  product_name: string;
  description: string;
  price: string;
  quantity: string;
  category_id: string;
  unique_code: string;
  status: string;
}

export interface VariantState {
  id: string;
  size: string;
  price: string;
  quantity: string;
}
